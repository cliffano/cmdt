{
  "commands": {
    "init": {
      "desc": "Create sample test files"
    },
    "run": {
      "desc": "Execute test files",
      "options": [
        { "arg": "-d, --debug", "desc": "Display debug info on failing tests and keep execution directory, default: false" },
        { "arg": "-b, --base-dir <baseDir>", "desc": "Base directory where commands will be executed, default: os.tempdir() "}
      ],
      "examples": [
        "cmdt run test1.cmdt test2.cmdt test3.cmdt"
      ]
    }
  }
}